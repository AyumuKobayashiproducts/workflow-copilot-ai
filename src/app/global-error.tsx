"use client";

// Sentry recommends adding a global error handler in App Router to capture React render errors.
// We reuse the existing UI from `app/error.tsx`.
import ErrorComponent from "./error";

export default ErrorComponent;


